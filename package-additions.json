{
  "description": "Add these dependencies to your package.json",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.9.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "canvas": "^2.11.2",
    "uuid": "^9.0.1",
    "pg": "^8.11.3",
    "winston": "^3.11.0",
    "node-cron": "^3.0.3",
    "twilio": "^4.19.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "nodemon": "^3.0.2"
  },
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "jest",
    "migrate": "psql $DATABASE_URL < migrations/add_transaction_support.sql && psql $DATABASE_URL < migrations/add_blueprint_analysis.sql",
    "cleanup": "node -e \"require('./src/utils/fileUpload').cleanupOldFiles(90)\""
  }
}
