{
  "name": "@pipelineos/contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Shared API contracts and schemas",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.4",
    "joi": "^17.11.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
