/**
 * Shared API Contracts and Schemas
 */

const leadsSchema = require('./leads.schema');
const blueprintsSchema = require('./blueprints.schema');
const bidsSchema = require('./bids.schema');
const responsesSchema = require('./responses.schema');

module.exports = {
  leadsSchema,
  blueprintsSchema,
  bidsSchema,
  responsesSchema,
};
