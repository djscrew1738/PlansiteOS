# PlansiteOS Frontend - Production Environment Variables
# Copy this to .env.production and fill in your values

# API base URL (leave empty if frontend is served from same origin as API)
VITE_API_BASE=

# Application version (set during CI/CD)
VITE_APP_VERSION=3.0.0

# Enable/disable service worker
VITE_ENABLE_SW=true
